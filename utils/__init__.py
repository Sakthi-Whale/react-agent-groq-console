# utils/__init__.py
#
# Marks this directory as a Python package so `from utils.helpers import ...`
# works correctly when app.py imports it. Left intentionally empty - no
# package-level initialization is needed for this project.
